[package]
name = "no-way-jose-core"
version = "0.1.0-rc.1"
edition = "2024"
description = "Core traits, token types, and JWK support for the no-way-jose JOSE library"
readme = "README.md"

[dependencies]
base64ct = { workspace = true }
error-stack = { workspace = true }
